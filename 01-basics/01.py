# Write a python code that prints the lenght of the strings

word_list = [
    "",         # 0
    "H",        # 1
    "Hello",    # 5
    "Amazing"   # 7
]

for word in word_list:
    print(f'"{word}":', len(word))
